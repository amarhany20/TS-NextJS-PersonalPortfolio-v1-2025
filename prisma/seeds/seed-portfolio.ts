import { PrismaClient } from '@prisma/client';

export async function seedPortfolio(prisma: PrismaClient) {
  console.log('\n[portfolio] Skipping portfolio seeding (placeholder). No projects inserted.');
  // Intentionally left blank for future project case study ingestion.
}
