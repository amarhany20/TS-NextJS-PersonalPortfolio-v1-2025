# 14. CI/CD

- GitHub Actions: install → typecheck → lint → test → build.
- Block merges on failing checks.
- Preview deployments for PRs.
- Run `prisma migrate deploy` during production releases before flipping traffic.

---
