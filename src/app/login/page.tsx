export default function LoginPlaceholder() {
  return (
    <section className="mx-auto max-w-lg space-y-6 py-16 text-center">
      <h1 className="text-4xl font-semibold">Admin Login</h1>
      <p className="text-muted-foreground">
        The admin login UI is coming soon. Use the <code className="mx-1">/api/v1/auth/login</code> endpoint to create an authenticated session while the interface is under construction.
      </p>
    </section>
  );
}
