export default function AdminSkillsPage() {
  return (
    <section className="space-y-3">
      <header>
        <h1 className="text-2xl font-semibold">Skills</h1>
        <p className="text-sm text-[var(--text-secondary)]">Organise skill groups and keep the stack representation current.</p>
      </header>
      <div className="rounded-lg border border-dashed border-[var(--border)] bg-[var(--card-bg)]/40 p-6 text-sm text-[var(--text-secondary)]">
        Skill group management UI will plug in here shortly.
      </div>
    </section>
  );
}
