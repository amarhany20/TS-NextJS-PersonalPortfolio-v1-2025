'use client';

export default function Footer() {
  // Footer intentionally minimal. The app version is displayed in the left profile sidebar.
  return <footer className="mt-6 text-[10px] text-center text-[var(--text-secondary)]" />;
}
