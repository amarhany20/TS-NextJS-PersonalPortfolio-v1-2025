# `src/sections`

Purpose: page-scoped sections that compose reusable components into public-page layouts.

Rules:

- Keep sections focused on presentation and light orchestration.
- Avoid hiding business rules in sections; move durable data logic to services or server components.
- Keep public-page copy and structure aligned with active launch docs.
