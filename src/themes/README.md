# `src/themes`

Purpose: theme registry metadata and shared theme typing for the portfolio shell.

Rules:
- Keep the theme registry descriptive and launch-ready; no placeholder authoring data.
- Make preview/apply behavior match the admin theme docs and tests.
- Prefer typed theme metadata over ad-hoc shape changes in components.
